# Number Adder

This is a simple Python project that adds two numbers. It demonstrates basic input and output in Python.

## How to Use

1. Run the `main.py` script in your Python environment.

2. You will be prompted to enter two numbers.

3. The script will add the numbers and display the result.

## Example

